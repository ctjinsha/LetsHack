class Solution:
    def getCommon(self, nums1: List[int], nums2: List[int]) -> int:
        
        res=set(nums1)
        for i in nums2:
            if i in res:
                return i
        return -1

        
